# Wayfinder backend — pinned core dependencies
fastapi==0.115.6
uvicorn[standard]==0.34.0
# pydantic>=2.12.5 is required by google-genai==2.10.0; keep <3.0.0 for fastapi
# and >=2.7.0 for pydantic-settings. 2.12.5 is the lowest pin satisfying all three.
pydantic==2.12.5
pydantic-settings==2.7.1

# Database
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
alembic==1.14.0

# Cache
redis==5.2.1

# Auth (Supabase JWT verification)
pyjwt[crypto]==2.10.1
httpx==0.28.1

# Orchestration + optimization
langgraph==0.2.60
ortools==9.15.6755

# Optional LLM vendor SDK (Gemini). Uses google-genai (the supported SDK);
# NOT the deprecated google-generativeai, which pins protobuf<6 and conflicts
# with ortools. Claude/GPT SDKs (anthropic, openai) remain optional extras.
google-genai==2.10.0

# Vector store / RAG
pgvector==0.3.6

# MCP server (demonstrative Maps/Weather tools)
mcp==1.28.0

# Testing
pytest==8.3.4
hypothesis==6.123.2
